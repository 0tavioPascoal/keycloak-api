spring:
  application:
    name: keycloak-api

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/keycloak-spring

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info